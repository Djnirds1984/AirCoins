{
    "version":  "v1.29.13",
    "release_notes":  "- **Removal: Sub-Vendo (NodeMCU) support deleted entirely â€” GPIO is the only coinslot.** The SBC\u0027s GPIO coin listener and the NodeMCU sub-vendo feature were fighting over the same coin pipeline: the `auto` coinslot resolution preferred a remote unit over GPIO, coins could be credited under `subvendo:\u003cid\u003e` sources the GPIO-only start path then failed to find, and the admin GPIO master toggle was entangled with the NodeMCU arm/approval lifecycle. Sub-vendo is now gone: the `/api/subvendo/*` device routes, `/api/admin/subvendos` + `/api/admin/ssid-vlan-map` admin routes, `handlers/subvendo.go`, the admin **Sub-Vendos** page and **Settings â†’ SSID/VLAN Map** card, the portal\u0027s \"Select Vendo Machine\" picker, the `firmware/` NodeMCU sketch, and the `sub_vendos` / `ssid_vlan_map` tables (dropped at startup by EnsureSchema and by migrations.sql) are all removed. Arm/status/start now always target `local_gpio`, so Done Paying credits the coins the GPIO listener recorded â€” the GPIO toggle simply starts/stops the listener again.\n- Coinslot API compatibility: `/api/coinslot/arm|status|disarm|options` still accept the old `coinslot` parameter and `options` still returns a `subvendos: []` field, so cached portal pages keep working until they refresh â€” everything resolves to the GPIO coinslot.",
    "sha256":  "09d519ec255f77209780494862c1cd0cbd6ba429fbd8af9cec05a1f8a905c4e9",
    "tarball":  "releases/aircoins-v1.29.13.tar.gz",
    "versions":  [
                     {
                         "released_at":  "2026-09-14T12:17:46Z",
                         "sha256":  "09d519ec255f77209780494862c1cd0cbd6ba429fbd8af9cec05a1f8a905c4e9",
                         "version":  "v1.29.13",
                         "release_notes":  "- **Removal: Sub-Vendo (NodeMCU) support deleted entirely â€” GPIO is the only coinslot.** The SBC\u0027s GPIO coin listener and the NodeMCU sub-vendo feature were fighting over the same coin pipeline: the `auto` coinslot resolution preferred a remote unit over GPIO, coins could be credited under `subvendo:\u003cid\u003e` sources the GPIO-only start path then failed to find, and the admin GPIO master toggle was entangled with the NodeMCU arm/approval lifecycle. Sub-vendo is now gone: the `/api/subvendo/*` device routes, `/api/admin/subvendos` + `/api/admin/ssid-vlan-map` admin routes, `handlers/subvendo.go`, the admin **Sub-Vendos** page and **Settings â†’ SSID/VLAN Map** card, the portal\u0027s \"Select Vendo Machine\" picker, the `firmware/` NodeMCU sketch, and the `sub_vendos` / `ssid_vlan_map` tables (dropped at startup by EnsureSchema and by migrations.sql) are all removed. Arm/status/start now always target `local_gpio`, so Done Paying credits the coins the GPIO listener recorded â€” the GPIO toggle simply starts/stops the listener again.\n- Coinslot API compatibility: `/api/coinslot/arm|status|disarm|options` still accept the old `coinslot` parameter and `options` still returns a `subvendos: []` field, so cached portal pages keep working until they refresh â€” everything resolves to the GPIO coinslot.",
                         "tarball":  "releases/aircoins-v1.29.13.tar.gz"
                     },
                     {
                         "released_at":  "2026-09-14T11:24:23Z",
                         "sha256":  "827fa867baa164221e6801733b0fd3d884323b70a9f00620a9b78b8d69c76ff9",
                         "version":  "v1.29.12",
                         "release_notes":  "- **Fix: \"Failed to start session\" after Done Paying on multi-vendo / sub-Vendo deployments** â€” The portal now locks onto the coinslot the server *actually* armed (the `/api/coinslot/arm` response echoes the resolved `gpio` or `subvendo:N`) and threads that same slot through the countdown status poll, the disarm call, and the `/api/session/start` call. Previously the status poll and start re-resolved the slot independently, so on boxes with remote Sub-Vendo units the portal could look up coins under a different source than the one inserted into â€” leaving the server with \"no credited coins\" (HTTP 400) at Start. Now arm/status/start all agree on one source.\n- **Fix (diagnostics/self-heal): non-JSON replies to `/api/session/start` are retried once instead of instantly showing the generic failure.** When the lighttpd `error-handler-404` fallback answers a start POST with HTTP 200 + portal HTML â€” the signature of a broken/old `aircoins-api` process (e.g. the corrupted v1.29.9 binary documented below) â€” the portal retries once before reporting. If the API is truly not answering JSON, the error you see is the *real* cause: the Go API isn\u0027t running or is serving HTML, so check `systemctl status aircoins-api` / restore the API binary (see v1.29.10 notes) rather than re-tapping coins.",
                         "tarball":  "releases/aircoins-v1.29.12.tar.gz"
                     },
                     {
                         "released_at":  "2026-09-13T03:41:45Z",
                         "sha256":  "471dd669988dd2990b9cb7e6ccc08bc8511412ebaec9a49e9dfb087df33a573a",
                         "version":  "v1.29.11",
                         "release_notes":  "- **Fix: GPIO toggle showed OFF even when the listener was running** â€” The `listener_active` check added in v1.29.6 under-reported when the CGI ran as www-data (`systemctl is-active` and `kill -0` both fail with EPERM on the root-owned listener), and the \"live state wins\" rule then flipped the toggle OFF despite the saved config saying ON. The CGI now detects the listener via the world-readable `/proc/\u003cpid\u003e/cmdline` (works for any user), and the admin panel treats the saved `GPIO_ENABLED` flag as authoritative â€” `listener_active` is only a fallback when no config exists, plus the drift-notice hint.",
                         "tarball":  "releases/aircoins-v1.29.11.tar.gz"
                     },
                     {
                         "released_at":  "2026-09-13T03:17:08Z",
                         "sha256":  "db758e1a0b8eeb451fef5420d36377d66d4f8ac08442645018b1272885e1dc58",
                         "version":  "v1.29.10",
                         "release_notes":  "- **Fix (critical): OTA no longer ships a corrupted API binary** â€” The line-ending normalization added in v1.29.9 ran sed over system/usr/local/bin recursively, which stripped \\\\r bytes from the compiled ARM/x64 ELF binaries and corrupted them (\"too large section header offset\"). Devices that installed v1.29.9 ended up with an aircoins-api that cannot start (no login, all /api requests answered with HTML by the portal fallback). The normalizer now explicitly excludes the compiled binaries. If your device is stuck on the broken v1.29.9: OTA is impossible while the API is down â€” restore manually by downloading this release tarball and copying system/usr/local/bin/aircoins-api/aircoins-api over /usr/local/bin/aircoins-api/aircoins-api, then `systemctl restart aircoins-api`.",
                         "tarball":  "releases/aircoins-v1.29.10.tar.gz"
                     },
                     {
                         "released_at":  "2026-09-13T03:06:53Z",
                         "sha256":  "cfb3d121a7ba5cd053076622fdba5f5176d12d0d39d46c4412725df51763dd4d",
                         "version":  "v1.29.9",
                         "release_notes":  "- **Fix: CGI script had Windows CRLF line endings (\"cannot execute: required file not found\")** â€” The rewritten get_gpio_config shipped with CRLF, so its shebang was \"#!/bin/bash\\r\" and Linux refused to run it (lighttpd answered 200 with an empty body; running the script directly printed \"cannot execute: required file not found\"). The script is now LF-only, and build-release.sh strips CR from every packaged Linux script/CGI (with a backup-safe pass over install.sh, recover script and docs) so a Windows edit can never ship a broken script again.",
                         "tarball":  "releases/aircoins-v1.29.9.tar.gz"
                     }
                 ],
    "released_at":  "2026-09-14T12:17:46Z"
}